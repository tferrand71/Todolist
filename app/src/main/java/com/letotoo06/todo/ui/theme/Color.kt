package com.letotoo06.todo.ui.theme

import androidx.compose.ui.graphics.Color

// --- THÈME CLAIR (Crème) ---
val CreamBackground = Color(0xFFFAF8F5) // Un blanc légèrement beige/crème
val CreamSurface = Color(0xFFFFFFFF)    // Blanc pur pour les cartes (ressort sur la crème)
val TextDark = Color(0xFF2D2D2D)        // Gris très foncé (plus doux que le noir pur)
val PrimaryCream = Color(0xFF8D6E63)    // Un marron/taupe élégant pour les boutons

// --- THÈME SOMBRE ---
val DarkBackground = Color(0xFF121212)  // Noir standard Material Design
val DarkSurface = Color(0xFF1E1E1E)     // Gris très foncé pour les cartes
val TextLight = Color(0xFFE0E0E0)       // Blanc cassé pour le texte
val PrimaryDark = Color(0xFFBCAAA4)     // Version plus claire du taupe pour contraster